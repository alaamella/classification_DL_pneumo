# Chest X-Ray Dataset Structure

This directory is organized to host the chest X-ray dataset required by the training and evaluation notebooks. Populate the `train`, `val`, and `test` folders with the corresponding NORMAL and PNEUMONIA images before running any experiments.
