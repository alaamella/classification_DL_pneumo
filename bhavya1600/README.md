# bhavya1600 Contribution

`Chest_Pneumonia_Classification.ipynb` contains a TensorFlow/Keras pipeline with data augmentation, a custom CNN classifier, and evaluation utilities for the chest X-ray dataset in `../data/chest_xray`.
