# PLGaultier Contribution

This folder hosts the FastAI notebook `model.ipynb`, which fine-tunes a ResNet-34 backbone on the shared chest X-ray dataset. It expects the dataset to live in `../data/chest_xray` with the `train`, `val`, and `test` splits described in the project README.
